import { LoginClient } from './LoginClient';

export const metadata = {
  title: 'Sign In',
  description: 'Sign in to sync your watchlist, portfolio, and price alerts across devices.',
};

export default function LoginPage() {
  return <LoginClient />;
}
